package interfaces;
//Questa interfaccia sarà comune a Audio e Video. Contiene i metodi per alzare e abbassare il volume (da 1 a 5)//

public interface SetVolume {
   void volumeUp();

    void volumeDown();
}
